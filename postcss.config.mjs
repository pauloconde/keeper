// Standard PostCSS config for Next.js + TailwindCSS
// Use the plugin names as keys with empty option objects to match Next.js expectations.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};
